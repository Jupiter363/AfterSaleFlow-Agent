/**
 * Selects TRANSFERRED, SIMPLE_HEARING, or FULL_HEARING from deterministic business facts.
 *
 * <p>Routing cannot create a remedy, approve a decision, or invoke an execution tool.
 */
/*
 * 所属模块：听证准入路由。
 * 文件职责：说明「com.example.dispute.routing」包在系统中的职责与禁止依赖。
 * 业务链路：供架构检查、IDE 和 Javadoc 展示包级边界；依据争点、证据充分度和风险把案件分入三种最终听证路线。
 * 关键边界：路由算法必须确定、可测试且可解释，不能在此阶段生成最终赔付决定
 */
/*
 * 所属模块：听证准入路由。
 * 文件职责：说明「com.example.dispute.routing」包在系统中的职责与禁止依赖。
 * 业务链路：供架构检查、IDE 和 Javadoc 展示包级边界；依据争点、证据充分度和风险把案件分入三种最终听证路线。
 * 关键边界：路由算法必须确定、可测试且可解释，不能在此阶段生成最终赔付决定
 */
/*
 * 所属模块：听证准入路由。
 * 文件职责：说明「com.example.dispute.routing」包在系统中的职责与禁止依赖。
 * 业务链路：供架构检查、IDE 和 Javadoc 展示包级边界；依据争点、证据充分度和风险把案件分入三种最终听证路线。
 * 关键边界：路由算法必须确定、可测试且可解释，不能在此阶段生成最终赔付决定
 */
/*
 * 所属模块：听证准入路由。
 * 文件职责：说明「com.example.dispute.routing」包在系统中的职责与禁止依赖。
 * 业务链路：供架构检查、IDE 和 Javadoc 展示包级边界；依据争点、证据充分度和风险把案件分入三种最终听证路线。
 * 关键边界：路由算法必须确定、可测试且可解释，不能在此阶段生成最终赔付决定
 */
/*
 * 所属模块：听证准入路由。
 * 文件职责：说明「com.example.dispute.routing」包在系统中的职责与禁止依赖。
 * 业务链路：供架构检查、IDE 和 Javadoc 展示包级边界；依据争点、证据充分度和风险把案件分入三种最终听证路线。
 * 关键边界：路由算法必须确定、可测试且可解释，不能在此阶段生成最终赔付决定
 */
/*
 * 所属模块：听证准入路由。
 * 文件职责：说明「com.example.dispute.routing」包在系统中的职责与禁止依赖。
 * 业务链路：供架构检查、IDE 和 Javadoc 展示包级边界；依据争点、证据充分度和风险把案件分入三种最终听证路线。
 * 关键边界：路由算法必须确定、可测试且可解释，不能在此阶段生成最终赔付决定
 */
/*
 * 所属模块：听证准入路由。
 * 文件职责：说明「com.example.dispute.routing」包在系统中的职责与禁止依赖。
 * 业务链路：供架构检查、IDE 和 Javadoc 展示包级边界；依据争点、证据充分度和风险把案件分入三种最终听证路线。
 * 关键边界：路由算法必须确定、可测试且可解释，不能在此阶段生成最终赔付决定
 */
/*
 * 所属模块：听证准入路由。
 * 文件职责：说明「com.example.dispute.routing」包在系统中的职责与禁止依赖。
 * 业务链路：供架构检查、IDE 和 Javadoc 展示包级边界；依据争点、证据充分度和风险把案件分入三种最终听证路线。
 * 关键边界：路由算法必须确定、可测试且可解释，不能在此阶段生成最终赔付决定
 */
package com.example.dispute.routing;
