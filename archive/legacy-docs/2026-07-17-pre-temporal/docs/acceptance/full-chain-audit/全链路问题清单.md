# 全链路问题清单

执行范围：正式版全链路筛查。登录体系暂不验收，统一使用前端顶部“身份 ID + 体验角色”下拉栏切换用户、商家、平台审核员。

验收样本：`CASE_66a0d3c7332f4c329daa534c7df2d717`  
验收时间：2026-07-05 04:08-04:20（Asia/Shanghai）

## 汇总

| 严重级别 | 数量 | 状态 |
|---|---:|---|
| Blocker | 0 | 无 |
| Critical | 3 | 已修复 |
| Major | 3 | 2 已修复，1 已缓解 |
| Minor / UX | 2 | 1 已修复，1 待后续优化 |

## 问题明细

| 编号 | 级别 | 模块 | 现象 | 根因 | 处理状态 | 验证结果 |
|---|---|---|---|---|---|---|
| ENV-001 | Major | 前端运行环境 | 浏览器看到的页面和 main 代码不一致 | Vite 曾从旧 worktree 启动 | 已修复：前端从 `D:\学习\Project\AfterSaleFlow-Agent\frontend` 启动 | `http://127.0.0.1:5173/disputes` 访问正常 |
| ENV-002 | Critical | Java ↔ Python Agent | 本地 Java 调用 Agent 降级，模型对话结果不可用 | `application-local.yml` 默认指向 Docker 内网 `python-agent-service:8000` | 已修复：local 默认改为 `http://127.0.0.1:8000`，`.env` 同步 | 接待/庭审无 Internal Server Error；Agent 不可用时进入确定性兜底 |
| ENV-003 | Critical | Temporal Worker | 第三轮庭审封存后未生成裁决草案/审核任务 | 本地 Java 启动时关闭了 Temporal Worker | 已修复：local 默认 `APP_TEMPORAL_WORKER_ENABLED=true` | worker 注册成功，第三轮后生成 `REMEDY_767a...` 和 `REVIEW_4e2f...` |
| ENV-004 | Major | Temporal 历史兼容 | 启动 worker 后消费旧重构前 history，出现 `NonDeterministicException` | 旧工作流历史与重构后的 workflow 命令序列不兼容 | 已缓解：本地 task queue 版本化为 `case-dispute-task-queue-v2` | 新 worker 日志显示 v2 队列；不再消费旧队列历史 |
| BE-001 | Critical | 庭审终局 | Agent 不可用时，最终轮次曾生成 `MANUAL_REVIEW_REQUIRED`，不是确定方案 | fallback 未区分“中途轮次”和“最终收束” | 已修复：最终收束兜底输出 `RESHIP/REFUND/REPLACE` 等可审核方案 | Java 回归测试通过；E2E 生成 `RESHIP_IF_SIGNATURE_PROOF_MISSING` |
| FE-001 | Major | 审核工作台 | ReviewPacket 有草案但 UI 显示“等待草案” | 前端只读取 `conclusion/decision`，未读取后端 `recommended_decision/draft_text` | 已修复：兼容 snake_case/camelCase/旧字段 | 前端单测通过；浏览器确认显示 `RESHIP_IF_SIGNATURE_PROOF_MISSING` |
| UX-001 | Minor | 小法庭页面 | 第三轮封存后页面停留在庭审卷轴，没有主动跳转/提示进入终审 | 前端未对“已进入平台终审”事件做明显状态切换 | 已修复：最终轮封存后展示平台终审交接卡片，并监听审核门事件切换为已送审状态 | `HearingCourtView.test.js` 12/12 passed；主链路不再出现庭审页空等待 |
| LOG-001 | Minor | 结案评价/日志 | 终审后结案评价 Agent 503 时产生 warn，但执行动作和结案成功 | 结案评价属于辅助 Agent，缺少更清晰的降级日志分级 | 待优化 | 案件已 CLOSED，4 项执行动作均 SUCCEEDED |
