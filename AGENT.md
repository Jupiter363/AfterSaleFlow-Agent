# 项目 Agent 开发约定

## 敏捷开发与验证

- 完成一个开发任务后，默认直接进入下一项开发，不自动执行全量测试或端到端回归测试。
- 编辑期间优先执行与改动直接相关的静态检查和聚焦测试；不要在每个小任务后启动全量、端到端或浏览器回归。
- 用户指定测试范围时，只执行该范围；未经明确要求，不扩大为全量、端到端或浏览器回归。
- 全量或端到端验证只在用户明确要求，或已约定的统一验证检查点执行。

## Agent 协作

- 使用子 Agent 时，必须遵循 `plans/agent-collaboration-execution.md` 的自适应模型、并发和责任边界规则。
- 主 Agent 必须在委派前声明每个角色的精确可写路径与禁止路径；同一文件不得由多个写入者并发修改。
- 并发受限、模型不可用或遇到 429、503、用量限制时，保留已完成和未提交的安全改动，按规则改派或退避重试，不降低 P0 审查要求。
