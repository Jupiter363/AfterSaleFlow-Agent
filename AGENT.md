# 项目 Agent 开发约定

## 工作边界

- `main` 是当前生产基线；优先修复责任机制，不使用案件 ID、魔法前缀或环境特例绕过。
- 未经用户明确授权，不升级或重启 Temporal、PostgreSQL、Redis、Elasticsearch、MinIO、
  LiteLLM 等核心组件。
- 保留数据库 migration、版本化协议、Temporal replay fixture 和兼容代码；不能仅因版本旧
  或类名含 legacy 就删除。
- 文档统一放在 `docs/`；根 `README.md` 和 Agent 约束文件是仓库入口例外。
- 保留用户已有修改，禁止用破坏性 Git 命令覆盖工作树。

## 开发与验证

- 编辑期间只执行与改动直接相关的静态检查和聚焦测试。
- 全量测试、浏览器 E2E 或全服务重启只在用户明确要求或统一发布检查点执行。
- 本地开发默认端口：前端 `5173`、Java `8080`、Python `18000`；Docker 是最终全服务形态。
- Bug 修复必须说明根因、被破坏的不变量、一般化修复和防回归测试。
- 涉及协议分支时必须由显式权威 discriminator 驱动，并覆盖正向、负向、幂等/重放和
  相邻回归。

## 协作

- 委派前必须声明精确可写路径、禁止路径、验收条件和交付格式。
- 同一文件不得由多个写入者并发修改；主 Agent 负责集成、最终验证和外部变更。
- 子任务不得扩大删除、秘密、生产写入、发布或核心组件操作权限。
