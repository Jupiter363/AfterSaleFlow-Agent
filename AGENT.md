# 项目 Agent 开发约定

## 敏捷开发与验证

- 完成一个开发任务后，默认直接进入下一项开发，不自动执行全量测试或端到端回归测试。
- 不因 Agent 自行判断“应该验证”而启动测试；只有用户明确下达测试指令时才运行对应测试。
- 用户指定测试范围时，只执行该范围；未经明确要求，不扩大为全量、端到端或浏览器回归。
- 未收到测试指令时，可以进行代码审阅、静态检查和变更范围核对，但不启动测试命令。
