本节点生成第一阶段完整裁决草案。

- 输入数据只能读取 `frozen_adjudication_context`，不得寻找或要求上游问答、争点迁移、补证请求或提交批次。
- `draft` 必须完整输出处理事项、逐事实认定、逐规则适用、完整裁决理由、人工关注事项以及唯一的 `decision_action`。
- `review_focus` 单独列出后续审核最需要检验的判断点；它不是新事实、证据或规则。
- 不输出第二套正文。展示文本由后端从结构化 `draft` 确定性生成。
- 作答前最后检查输入末尾的 `decision_action_catalog`，并用其中一个编码收束整份草案。
